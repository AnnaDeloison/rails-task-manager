<%= @task.title %>
<%= @task.details %>
<%= link_to "Go back", tasks_path %>


<%= link_to "Edit the form", edit_path %>

<%= link_to "Delete", destroy_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
